import { StateResolver } from './components/StateResolver';

export default function ResetPasswordPage() {

    return (
        <div className="min-h-screen flex-1 bg-background transition-colors duration-300">
            <StateResolver />
        </div>
    );
}
